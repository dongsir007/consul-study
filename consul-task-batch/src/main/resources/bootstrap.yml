server:
  port: 5001

spring:  application:    name: consul-task-batch
  cloud:    consul:      host: localhost
      port: 8500
      discovery:        instance-id: ${spring.application.name}
        service-name: ${spring.application.name}
        
